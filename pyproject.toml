[project]
name = "smart-crop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Josh Kallus", email = "joshk.kallus3@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=2.48.0",
    "pillow>=12.3.0",
]

[project.scripts]
smart-crop = "smart_crop:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
